# 📝 文章测试题和音频功能说明

## 🎯 问题解答

### 1. 测试题数据来源

#### 数据存储位置
测试题数据存储在 **数据库的 `articles` 表的 `quiz` 字段中**。

#### 数据来源
测试题来自于 `backend/src/scripts/articles.json` 文件，在运行 `npm run db:seed` 时被导入到数据库。

#### 数据结构示例
```json
{
  "title": "我的一天",
  "quiz": [
    {
      "question": "作者早上几点起床？",
      "options": ["六点", "七点", "八点", "九点"],
      "answer": 1,
      "explanation": "文章说：早上七点，我起床"
    },
    {
      "question": "作者是做什么的？",
      "options": ["老师", "学生", "医生", "司机"],
      "answer": 1,
      "explanation": "文章说：我是学生"
    }
  ]
}
```

#### 数据流程
```
1. articles.json (源文件)
   ↓
2. seed.js 读取并处理
   ↓
3. 存入数据库 articles.quiz 字段 (JSON string)
   ↓
4. API 返回给前端
   ↓
5. 前端解析并显示测试题
```

---

### 2. 音频播放功能

#### 当前状态
✅ 已添加音频播放功能
⚠️ 暂无实际音频文件

#### 功能实现
```typescript
<Button 
  onClick={() => {
    if (article?.audioUrl) {
      const audio = new Audio(article.audioUrl);
      audio.play().catch(err => {
        alert('暂无音频文件');
      });
    } else {
      alert('暂无音频文件');
    }
  }}
  disabled={!article?.audioUrl}
>
  <Volume2 /> Play Audio
</Button>
```

#### 为什么音频不能播放？
**原因**：数据库中的 `audioUrl` 字段为 `NULL`，因为 `articles.json` 中没有提供音频URL。

```bash
# 数据库查询结果
sqlite> SELECT audioUrl FROM articles WHERE slug = 'wo-de-yi-tian';
(empty - NULL)
```

---

## 🎵 如何添加音频功能

### 方案1: 使用 Web Speech API (推荐 - 免费)

浏览器内置的语音合成功能，无需外部音频文件。

```typescript
const playAudio = (text: string, lang = 'zh-CN') => {
  const utterance = new SpeechSynthesisUtterance(text);
  utterance.lang = lang;
  utterance.rate = 0.8; // 播放速度
  window.speechSynthesis.speak(utterance);
};

// 使用
playAudio('早上七点，我起床。');
```

**优点**：
- 完全免费
- 无需音频文件
- 支持多种语言
- 可调节速度和音调

**缺点**：
- 音质不如真人
- 不同浏览器效果不同

---

### 方案2: 使用第三方 TTS 服务

#### A. Azure Cognitive Services (推荐)
```typescript
// 配置
const AZURE_SPEECH_KEY = 'your-key';
const AZURE_REGION = 'eastasia';

// API调用
const generateAudio = async (text: string) => {
  const response = await fetch(
    `https://${AZURE_REGION}.tts.speech.microsoft.com/cognitiveservices/v1`,
    {
      method: 'POST',
      headers: {
        'Ocp-Apim-Subscription-Key': AZURE_SPEECH_KEY,
        'Content-Type': 'application/ssml+xml',
        'X-Microsoft-OutputFormat': 'audio-16khz-128kbitrate-mono-mp3',
      },
      body: `
        <speak version='1.0' xml:lang='zh-CN'>
          <voice name='zh-CN-XiaoxiaoNeural'>
            ${text}
          </voice>
        </speak>
      `,
    }
  );
  return await response.blob();
};
```

**费用**：
- 每月前 500,000 字符免费
- 超出部分: $15/百万字符

---

#### B. Google Cloud Text-to-Speech
```typescript
const googleTTS = async (text: string) => {
  const response = await fetch(
    'https://texttospeech.googleapis.com/v1/text:synthesize',
    {
      method: 'POST',
      headers: {
        'Content-Type': 'application/json',
        'X-Goog-Api-Key': 'your-api-key',
      },
      body: JSON.stringify({
        input: { text },
        voice: { 
          languageCode: 'zh-CN', 
          name: 'zh-CN-Standard-A' 
        },
        audioConfig: { audioEncoding: 'MP3' },
      }),
    }
  );
  const data = await response.json();
  return data.audioContent; // Base64 encoded
};
```

**费用**：
- 每月前 100 万字符免费
- 标准语音: $4/百万字符
- WaveNet 语音: $16/百万字符

---

### 方案3: 预录音频文件

将文章朗读录制为 MP3 文件，上传到服务器。

#### 实施步骤

1. **录制音频**
   - 使用专业录音设备
   - 或使用 Azure/Google TTS 批量生成
   - 格式: MP3, 比特率: 128kbps

2. **存储音频**
   ```
   /public/audio/articles/
     - wo-de-yi-tian.mp3
     - zai-can-ting.mp3
     - wo-de-jia.mp3
   ```

3. **更新数据库**
   ```sql
   UPDATE articles 
   SET audioUrl = '/audio/articles/wo-de-yi-tian.mp3'
   WHERE slug = 'wo-de-yi-tian';
   ```

4. **前端已支持**
   - 只要 `audioUrl` 有值，按钮就会激活
   - 点击后自动播放

---

## 🚀 快速实施建议

### 立即可用方案（5分钟）

修改前端使用 Web Speech API：

```typescript
// frontend-user/app/dashboard/articles/[slug]/page.tsx

const playArticleAudio = () => {
  if ('speechSynthesis' in window) {
    // 停止之前的播放
    window.speechSynthesis.cancel();
    
    // 获取所有中文段落
    const paragraphs = content.map((p: any) => p.cn);
    const fullText = paragraphs.join('。 '); // 段落间加停顿
    
    const utterance = new SpeechSynthesisUtterance(fullText);
    utterance.lang = 'zh-CN';
    utterance.rate = 0.9; // 稍慢一点
    utterance.pitch = 1.0;
    
    utterance.onstart = () => {
      console.log('开始播放');
    };
    
    utterance.onend = () => {
      console.log('播放完成');
    };
    
    utterance.onerror = (event) => {
      console.error('播放失败:', event);
      alert('音频播放失败，请检查浏览器设置');
    };
    
    window.speechSynthesis.speak(utterance);
  } else {
    alert('您的浏览器不支持语音合成功能');
  }
};

// 修改按钮
<Button 
  variant="outline" 
  size="sm" 
  className="gap-2"
  onClick={playArticleAudio}
>
  <Volume2 className="h-4 w-4" />
  播放朗读
</Button>
```

---

## 📊 方案对比

| 方案 | 成本 | 音质 | 实施难度 | 推荐指数 |
|------|------|------|----------|----------|
| Web Speech API | 免费 | 一般 | 极低 | ⭐⭐⭐⭐ |
| Azure TTS | 低 | 优秀 | 中 | ⭐⭐⭐⭐⭐ |
| Google TTS | 低 | 优秀 | 中 | ⭐⭐⭐⭐ |
| 预录音频 | 高 | 最好 | 高 | ⭐⭐⭐ |

---

## 🎓 推荐实施路径

### 阶段1: MVP（立即）
✅ 使用 Web Speech API
- 5分钟实现
- 完全免费
- 立即可用

### 阶段2: 优化（1周内）
✅ 集成 Azure TTS
- 音质显著提升
- 成本可控
- 可批量生成

### 阶段3: 专业化（长期）
✅ 预录真人音频
- 最佳用户体验
- 需要投入资源
- 适合正式产品

---

## 💡 立即行动

如果您想**立即**启用音频功能，我可以帮您：

1. **实现 Web Speech API 方案**（5分钟）
   - 修改前端代码
   - 重启服务
   - 立即测试

2. **配置 Azure TTS**（需要您提供 API Key）
   - 更高质量的语音
   - 批量生成音频
   - 存储到数据库

您想使用哪个方案？我可以立即帮您实现！🚀


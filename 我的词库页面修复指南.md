# 我的词库页面修复指南

## 🐛 问题描述
用户访问 http://localhost:3001/dashboard/words 页面显示为空

## 🔍 问题诊断

### 1. 检查后端 API
已验证：✅ API 正常工作
```bash
# 测试结果：返回 2 个词汇
curl http://localhost:3000/api/user/words -H "Authorization: Bearer TOKEN"
```

### 2. 可能的原因
- ❓ 用户未登录或 token 过期
- ❓ 前端未正确获取或显示数据
- ❓ 浏览器控制台有错误

## 🔧 修复方案

### 已实施的修复

#### 1. 添加错误处理
在 `frontend-user/app/dashboard/words/page.tsx` 中添加了：
- 错误状态显示
- 重新登录按钮
- 调试日志输出

#### 2. 添加调试日志
```javascript
console.log('Words data:', wordsData);
console.log('Is loading:', isLoading);
console.log('Error:', error);
```

## 📝 用户操作步骤

### 步骤 1: 确保已登录
1. 访问 http://localhost:3001/login
2. 使用测试账号登录：
   - 邮箱：`user@demo.com`
   - 密码：`user123`

### 步骤 2: 清除浏览器缓存
1. 打开浏览器开发者工具（F12）
2. 右键点击刷新按钮 → 选择"清空缓存并硬性重新加载"

### 步骤 3: 检查控制台
1. 打开浏览器控制台（F12）
2. 访问 http://localhost:3001/dashboard/words
3. 查看控制台输出：
   - `Words data:` 应该显示词汇数据
   - `Error:` 应该是 null
   - 如果有错误，查看错误信息

### 步骤 4: 检查 Network 面板
1. 打开 Network 面板
2. 刷新页面
3. 查找 `/api/user/words` 请求
4. 检查：
   - 状态码应该是 200
   - 响应应该包含 `words` 数组
   - Headers 中应该有 `Authorization: Bearer TOKEN`

## 🔧 手动修复方法

### 如果看到 401 错误（未授权）

**原因**：Token 过期或未正确存储

**解决方法**：
1. 退出登录（清除 localStorage）
```javascript
// 在浏览器控制台执行
localStorage.clear();
window.location.href = '/login';
```

2. 重新登录

### 如果看到 Network Error

**原因**：后端服务未运行

**解决方法**：
```bash
cd /Users/keko/Downloads/chinese-learning-platform
./START-ALL.sh
```

### 如果数据为空但 API 正常

**原因**：用户词库确实为空

**解决方法**：添加一些词汇
1. 访问 http://localhost:3001/dashboard/hsk-library
2. 进入任意 HSK 级别
3. 点击任意词汇
4. 在词条详情页添加到词库（如果有相关按钮）

或者通过 API 手动添加：
```bash
# 获取 token
TOKEN=$(curl -s -X POST http://localhost:3000/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"email":"user@demo.com","password":"user123"}' | jq -r '.token')

# 获取一个词汇 ID
WORD_ID=$(curl -s "http://localhost:3000/api/words?level=1&limit=1" | jq -r '.words[0].id')

# 添加到词库
curl -X POST http://localhost:3000/api/user/words \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d "{\"wordId\":\"$WORD_ID\",\"source\":\"Manual\"}"
```

## 🧪 验证修复

### 检查列表
- [ ] 浏览器控制台无错误
- [ ] Network 面板显示 200 状态码
- [ ] 页面显示统计卡片（Total Words 显示数字）
- [ ] 页面显示词汇卡片或空状态提示

### 预期结果

#### 如果有词汇
页面应该显示：
- 4 个统计卡片（Total Words, To Learn, Learning, Mastered）
- 搜索和筛选框
- 3 个标签页（To Learn, Learning, Mastered）
- 词汇卡片列表

#### 如果词库为空
页面应该显示：
- 4 个统计卡片（全部显示 0）
- 空状态提示："No vocabulary to learn for now"
- 操作按钮："Browse HSK Library"、"Analyze Text"

#### 如果有错误
页面应该显示：
- 错误图标 ⚠️
- 错误信息
- "重新登录" 和 "刷新页面" 按钮

## 🆘 仍然无法解决？

### 提供以下信息
1. 浏览器控制台完整错误信息
2. Network 面板中 `/api/user/words` 请求的详细信息
3. console.log 输出内容
4. 页面截图

### 临时解决方案
直接通过 API 检查数据：
```bash
# 在终端执行
TOKEN=$(curl -s -X POST http://localhost:3000/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"email":"user@demo.com","password":"user123"}' | jq -r '.token')

echo "Token: $TOKEN"

curl -s http://localhost:3000/api/user/words \
  -H "Authorization: Bearer $TOKEN" | jq '.'
```

如果 API 返回正常数据，问题就在前端；如果 API 也有问题，需要检查后端。

---

**更新时间**: 2025-10-21  
**状态**: 已添加错误处理和调试日志


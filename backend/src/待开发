，
1、用户端的每日一文的详情页，改成左右结构可以吗，左边是正文，右边是词汇和测试。所有的设计需要围绕这个页面的seo来做， 
 2、text anaylzer页面，点击分析后，生成的页面要跟每日一文的页面一样，能否做到，其实应该可以，调用后台每日一文生成的类似的
 ai接口来生成。  
 3、学习中心模块删除
 4、每日一文详情页加入我的词库功能按钮没用。
 5、用户端hsk库点击具体的hsk1，还有其他，都不能拉取后台的数据。展示词汇详情页。
6、我的词库模块，
参考微信文件传输中的图片
点击学习进入具体的词汇页面

7、1、2两个模块中加入词库，则该词的状态会改成待学习，在我的词库的待学习中可以看到
    在这两个模块中点击立即学习，则进入词汇详情页面
    8、在词汇详情页面，有个已经掌握按钮，则词汇状态改为已掌握，在我的词库的已掌握中可以看到
       还有通过考试的词汇默认状态为已掌握。


9、后台管理管理页面删除词汇生成功能模块，词条落地页模块
对词汇库模块进行如下设计

1）支持批量导入词汇，逗号隔开，选择hsk级别
   导入仅需要入库词汇信息，词汇和导入时间，状态为待生成词条
   然后手动选择，支持单个和批量选择，生成词条。

生成词条的ai提示词参考文件
根据参考文件返回的数据，重新调整词条的数据库设计 ，要支持扩展性

生成词条需要有严格的进度条反馈，信息反馈，错误情况

生成玩词条后，通过js渲染html，操作栏显示预览，则跳转到相应的html页面，

词汇状态改为已生成词条
预览没问题后，点击发布按钮则，词条状态为已发布，在用户端hsk词库中可以查看到

词汇库，要有状态，hsk级别，等筛选按钮，



4、学习中心模块删除

 5、用户端hsk库点击具体的词汇，展示词汇详情页。
6、我的词库模块，
参考附图
点击学习进入具体的词汇页面

7、每日 一文 和分析模块的两个模块中加入词库，则该词的状态会改成待学习，在我的词库的待学习中可以看到
    在这两个模块中点击立即学习，则进入词汇详情页面
    8、在词汇详情页面，有个已经掌握按钮，则词汇状态改为已掌握，在我的词库的已掌握中可以看到
       还有通过考试的词汇默认状态为已掌握。


9、后台管理管理页面删除词汇生成功能模块，词条落地页模块
对词汇库模块进行如下设计

1）支持批量导入词汇，逗号隔开，选择hsk级别
   导入仅需要入库词汇信息，词汇和导入时间，状态为待生成词条
   然后手动选择，支持单个和批量选择，生成词条。

生成词条的ai提示词参考文件
根据参考文件返回的数据，重新调整词条的数据库设计 ，要支持扩展性

生成词条需要有严格的进度条反馈，信息反馈，错误情况

生成玩词条后，通过js渲染html，操作栏显示预览，则跳转到相应的html页面，

词汇状态改为已生成词条
预览没问题后，点击发布按钮则，词条状态为已发布，在用户端hsk词库中可以查看到

词汇库，要有状态，hsk级别，等筛选按钮，



file:///Users/keko/Downloads/chinese-learning-platform/backend/src/seo2.html
词汇详情参考这个


1、后台是不是少了一块用户单词学习情况的模块。这是一个saas系统。记录用户的学习情况。
2、每日一文详情页加入按钮无效，请修复。
3、词汇详情页面增加一个按钮：已掌握，收藏，加入学习，点击已掌握在我的词库已掌握中显示，加入学习，则显示在学习中，收藏则显示在收藏。
4、 2、text anaylzer页面，点击分析后，生成的页面要跟每日一文的页面一样，能否做到，其实应该可以，调用后台每日一文生成的类似的
 ai接口来生成。  先讨论方案
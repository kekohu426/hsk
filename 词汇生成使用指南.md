# 词汇生成功能使用指南 🎯

## 快速开始

### 1. 访问管理端
打开浏览器访问：**http://localhost:3002**

登录账号：
- 邮箱：`admin@demo.com`
- 密码：`admin123`

### 2. 进入词汇生成器
在左侧导航栏选择 **"HSK 词汇生成器"**

### 3. 配置生成参数

#### 选择 HSK 等级
点击对应的等级按钮（HSK 1-6）：
- **HSK 1**: 150 个基础词汇
- **HSK 2**: 300 个词汇
- **HSK 3**: 600 个词汇
- **HSK 4**: 1200 个词汇
- **HSK 5**: 2500 个词汇
- **HSK 6**: 5000 个词汇

#### 自定义提示词（可选）
在 "AI 提示词" 文本框中，您可以：
- 留空使用默认提示词
- 或输入自定义要求，例如：
  ```
  生成 10 个 HSK 1 关于食物的常用词汇，
  包括详细的例句和用法说明
  ```

### 4. 开始生成

点击 **"生成 10 个词汇"** 按钮

## 实时反馈说明 ⚡

生成过程中，您会看到以下进度提示：

### 阶段 1: 准备阶段
```
📋 正在准备生成请求...
```

### 阶段 2: 连接服务
```
🚀 正在连接 AI 服务...
```

### 阶段 3: AI 生成（最耗时）
```
🤖 GLM-4 正在生成词汇内容... (这可能需要 30-60 秒)
```
**重要**：请勿关闭页面，耐心等待

### 阶段 4: 处理结果
```
✅ 正在处理生成结果...
```

### 成功提示
```
✅ 成功生成 10 个词汇！
您可以查看右侧的生成结果，然后选择导出或发布
```

## 查看生成结果 📝

生成成功后，右侧面板会显示所有生成的词汇：

每个词汇包含：
- 中文字符：例如 "学习"
- 拼音：例如 "xuéxí"
- HSK 等级标签
- 英文释义：例如 "to study"

## 处理生成结果 💾

### 方式 1: 发布到数据库
点击右上角的 **"发布"** 按钮
- 词汇将保存到数据库
- 用户端可以立即访问这些词汇

### 方式 2: 导出为 JSON
点击右上角的 **"导出"** 按钮
- 下载 JSON 文件到本地
- 文件名格式：`hsk1-vocab-1729411200000.json`
- 可用于备份或批量导入

## 错误处理 🔧

如果生成失败，您会看到红色错误提示框，包含：

### 错误信息示例

#### 登录过期
```
❌ 生成失败
错误信息: 身份验证失败 (401)

💡 建议：
• 检查后端服务是否正常运行
• 确认 AI API Key 配置正确
• 尝试减少生成数量
• 查看浏览器控制台了解详细错误
```
**解决**：重新登录管理端

#### AI 服务不可用
```
❌ 生成失败
错误信息: 服务器错误 (500): AI service is temporarily unavailable

💡 建议：
• 检查后端服务是否正常运行
• 确认 AI API Key 配置正确
• 尝试减少生成数量
• 查看浏览器控制台了解详细错误
```
**解决**：检查 `.env` 文件中的 `GLM_API_KEY` 配置

#### 网络连接失败
```
❌ 生成失败
错误信息: 无法连接到后端服务器，请检查后端是否运行

💡 建议：
• 检查后端服务是否正常运行
• 确认 AI API Key 配置正确
• 尝试减少生成数量
• 查看浏览器控制台了解详细错误
```
**解决**：确保后端服务在运行（http://localhost:3000）

#### 请求超时
```
❌ 生成失败
错误信息: 请求超时，可能是因为生成内容过多或网络不稳定

💡 建议：
• 检查后端服务是否正常运行
• 确认 AI API Key 配置正确
• 尝试减少生成数量
• 查看浏览器控制台了解详细错误
```
**解决**：减少生成数量或检查网络连接

## 最佳实践 ⭐

### 1. 合理设置生成数量
- 首次测试：建议生成 5 个词汇
- 正式使用：可生成 10-20 个词汇
- 避免一次生成过多（>20）以免超时

### 2. 自定义提示词技巧
```
示例 1 - 主题相关：
生成 10 个 HSK 2 关于家庭的词汇

示例 2 - 场景相关：
生成 10 个 HSK 3 在餐厅常用的词汇

示例 3 - 详细要求：
生成 10 个 HSK 4 商务场景词汇，
每个词汇提供 3 个不同场景的例句
```

### 3. 分批生成
- 不要一次生成所有词汇
- 按主题或场景分批生成
- 便于管理和审核

### 4. 及时保存
- 生成成功后立即导出或发布
- 避免浏览器关闭导致数据丢失

## 常见问题 FAQ ❓

### Q: 生成需要多长时间？
**A**: 通常需要 30-60 秒，取决于：
- 生成数量
- AI 服务响应速度
- 网络状况

### Q: 可以同时生成多个等级吗？
**A**: 不可以，每次只能选择一个 HSK 等级

### Q: 生成失败后数据会丢失吗？
**A**: 是的，建议生成成功后立即保存

### Q: 如何修改已生成的词汇？
**A**: 当前版本不支持在生成页面编辑，建议：
1. 导出 JSON 文件
2. 手动编辑
3. 通过批量导入重新上传

### Q: 生成的词汇质量如何？
**A**: 使用 GLM-4 模型生成，质量较高，但建议：
- 人工审核内容准确性
- 必要时手动调整例句
- 验证拼音标注

## 技术支持 🛟

如遇到问题，可以：

1. **查看浏览器控制台**
   - 右键点击页面 → 检查 → Console
   - 查看详细错误信息

2. **查看后端日志**
   ```bash
   tail -f logs/backend.log
   ```

3. **检查服务状态**
   ```bash
   curl http://localhost:3000/health
   ```

4. **重启服务**
   ```bash
   ./STOP-ALL.sh
   ./START-ALL.sh
   ```

---

**祝您使用愉快！** 🎉

如有任何问题或建议，欢迎反馈。


# 词汇系统重构 - 详细开发计划

## 📊 项目概述

**目标**：重构词汇系统为单一词条架构，支持AI生成、预览、发布的完整工作流

**核心变化**：
- ❌ 删除：双表结构（Words + WordLandingPage）
- ✅ 新增：单一词条表（WordEntry）+ 完整生成工作流
- ✅ 保留：UserWord表（用户学习状态）

---

## 🗄️ 数据库设计

### 1. 新的词条表：WordEntry

```prisma
model WordEntry {
  id            String   @id @default(uuid())
  
  // 基础信息
  word          String   @unique  // 词汇本身（如"学校"）
  slug          String   @unique  // URL友好标识（如"xuexiao"）
  hskLevel      Int                // HSK等级 1-6
  
  // 内容（JSON存储，灵活扩展）
  contentJson   String            // AI生成的完整JSON内容
  
  // 状态管理
  status        String   @default("PENDING_IMPORT")  
  // 状态: PENDING_IMPORT(待生成) | GENERATING(生成中) | GENERATED(已生成) | PUBLISHED(已发布) | FAILED(生成失败)
  
  // AI生成相关
  aiPrompt      String?           // 生成时使用的提示词
  aiResponse    String?           // AI原始响应
  aiModel       String?           // 使用的AI模型
  generateError String?           // 生成失败的错误信息
  
  // 时间戳
  importedAt    DateTime @default(now())  // 导入时间
  generatedAt   DateTime?                 // 生成完成时间
  publishedAt   DateTime?                 // 发布时间
  updatedAt     DateTime @updatedAt
  
  // SEO字段
  seoScore      Float?            // SEO评分
  wordCount     Int?              // 内容字数
  
  // 关联
  userWords     UserWord[]        // 用户学习记录
  
  @@index([status])
  @@index([hskLevel])
  @@index([importedAt])
  @@map("word_entries")
}
```

### 2. 保留UserWord表（用户学习状态）

```prisma
model UserWord {
  id              String   @id @default(uuid())
  userId          String
  wordEntryId     String   // 关联到WordEntry
  
  // 学习状态
  status          String   @default("PENDING")
  // 状态: PENDING(待学习) | LEARNING(学习中) | MASTERED(已掌握)
  
  // SRS算法字段（后续扩展）
  level           Int      @default(0)
  repetitions     Int      @default(0)
  interval        Int      @default(0)
  nextReviewDate  DateTime?
  correctCount    Int      @default(0)
  wrongCount      Int      @default(0)
  
  // 笔记
  notes           String?
  
  // 时间戳
  addedAt         DateTime @default(now())
  lastReviewedAt  DateTime?
  updatedAt       DateTime @updatedAt
  
  // 关联
  user            User     @relation(fields: [userId], references: [id], onDelete: Cascade)
  wordEntry       WordEntry @relation(fields: [wordEntryId], references: [id], onDelete: Cascade)
  
  @@unique([userId, wordEntryId])
  @@index([userId, status])
  @@index([userId, nextReviewDate])
  @@map("user_words")
}
```

---

## 📋 详细开发任务列表

### 阶段1：数据库重构（2小时）

#### Task 1.1: 创建新的数据库schema
- [ ] 1.1.1 在`backend/prisma/schema.prisma`中添加`WordEntry`模型
- [ ] 1.1.2 修改`UserWord`模型，关联到`WordEntry`
- [ ] 1.1.3 删除旧的`Word`和`WordLandingPage`模型
- [ ] 1.1.4 运行`npx prisma migrate dev --name restructure_word_system`
- [ ] 1.1.5 验证数据库表创建成功

**文件**：`backend/prisma/schema.prisma`

#### Task 1.2: 清理旧数据
- [ ] 1.2.1 备份现有数据库（`cp prisma/dev.db prisma/dev.db.backup`）
- [ ] 1.2.2 清空旧表数据
- [ ] 1.2.3 验证新表为空

---

### 阶段2：Backend API开发（6小时）

#### Task 2.1: 词条导入API
**端点**：`POST /api/admin/words/import`

- [ ] 2.1.1 创建`backend/src/controllers/wordEntryController.js`
- [ ] 2.1.2 实现批量导入逻辑：
  ```javascript
  // 接收：words (逗号分隔), hskLevel
  // 处理：分割、去重、生成slug
  // 入库：status=PENDING_IMPORT
  ```
- [ ] 2.1.3 创建路由`backend/src/routes/wordEntry.js`
- [ ] 2.1.4 测试导入功能

**文件**：
- `backend/src/controllers/wordEntryController.js`
- `backend/src/routes/wordEntry.js`
- `backend/src/server.js`（注册路由）

#### Task 2.2: AI词条生成API
**端点**：`POST /api/admin/words/generate`

- [ ] 2.2.1 创建`backend/src/services/wordEntryAIService.js`
- [ ] 2.2.2 读取`backend/src/词条规则`作为提示词模板
- [ ] 2.2.3 实现生成逻辑：
  ```javascript
  // 1. 查询PENDING_IMPORT状态的词条
  // 2. 更新状态为GENERATING
  // 3. 调用AI生成JSON（使用GLM-4）
  // 4. 验证JSON格式
  // 5. 更新contentJson、status=GENERATED
  // 6. 如失败，更新status=FAILED + generateError
  ```
- [ ] 2.2.4 支持批量生成（队列处理）
- [ ] 2.2.5 实时进度推送（WebSocket或SSE）

**文件**：
- `backend/src/services/wordEntryAIService.js`
- `backend/src/controllers/wordEntryController.js`（添加generate方法）

#### Task 2.3: 词条列表API
**端点**：`GET /api/admin/words`

- [ ] 2.3.1 支持筛选：status、hskLevel、搜索关键词
- [ ] 2.3.2 支持排序：导入时间、HSK等级
- [ ] 2.3.3 支持分页

**文件**：`backend/src/controllers/wordEntryController.js`

#### Task 2.4: 词条详情API
**端点**：`GET /api/admin/words/:id`

- [ ] 2.4.1 返回完整词条信息
- [ ] 2.4.2 解析contentJson为对象

**文件**：`backend/src/controllers/wordEntryController.js`

#### Task 2.5: 词条发布API
**端点**：`POST /api/admin/words/:id/publish`

- [ ] 2.5.1 更新status=PUBLISHED
- [ ] 2.5.2 记录publishedAt时间

**文件**：`backend/src/controllers/wordEntryController.js`

#### Task 2.6: 词条删除API
**端点**：`DELETE /api/admin/words/:id`

- [ ] 2.6.1 软删除或硬删除（建议硬删除未发布的）
- [ ] 2.6.2 检查是否有用户关联

**文件**：`backend/src/controllers/wordEntryController.js`

#### Task 2.7: 用户端词条API
**端点**：`GET /api/words/:slug`

- [ ] 2.7.1 只返回已发布的词条（status=PUBLISHED）
- [ ] 2.7.2 如果用户已登录，返回学习状态
- [ ] 2.7.3 解析contentJson为对象

**文件**：`backend/src/controllers/wordEntryController.js`

#### Task 2.8: 用户学习状态API
**端点**：`POST /api/user/words/:wordEntryId/status`

- [ ] 2.8.1 添加到我的词库（status=PENDING）
- [ ] 2.8.2 更新状态（LEARNING/MASTERED）
- [ ] 2.8.3 添加笔记

**文件**：`backend/src/controllers/userWordController.js`

---

### 阶段3：管理端UI开发（8小时）

#### Task 3.1: 删除旧模块
- [ ] 3.1.1 删除`admin/src/pages/VocabGenerator.tsx`
- [ ] 3.1.2 删除`admin/src/pages/LandingPageGenerator.tsx`
- [ ] 3.1.3 删除`admin/src/pages/LandingPageManagement.tsx`
- [ ] 3.1.4 更新导航菜单

**文件**：
- `admin/src/App.tsx`（删除路由）
- `admin/src/components/Sidebar.tsx`（删除菜单项）

#### Task 3.2: 词汇管理页面
**页面**：`admin/src/pages/WordManagement.tsx`

**UI布局**：
```
┌─────────────────────────────────────────┐
│ 词汇管理                                 │
├─────────────────────────────────────────┤
│ [批量导入] [选择生成] [刷新]             │
│                                         │
│ 筛选：[状态v] [HSK等级v] [搜索🔍]        │
├─────────────────────────────────────────┤
│ ☑ | 词汇 | 拼音 | HSK | 状态 | 操作     │
│ ☑ | 学校 | xuexiao | 1 | 已生成 | [...] │
│ ☑ | 老师 | laoshi  | 1 | 待生成 | [...] │
└─────────────────────────────────────────┘
```

- [ ] 3.2.1 创建表格组件（支持复选框、筛选、排序）
- [ ] 3.2.2 状态Badge：
  - 待生成：灰色
  - 生成中：蓝色（旋转动画）
  - 已生成：绿色
  - 已发布：紫色
  - 生成失败：红色
- [ ] 3.2.3 操作列：预览、编辑、发布、删除

**文件**：`admin/src/pages/WordManagement.tsx`

#### Task 3.3: 批量导入对话框
**组件**：`admin/src/components/WordImportDialog.tsx`

```
┌────────────────────────────┐
│ 批量导入词汇                │
├────────────────────────────┤
│ HSK等级：[1v]               │
│                            │
│ 词汇列表（逗号分隔）：       │
│ ┌────────────────────────┐ │
│ │学校,老师,学生,书,桌子    │ │
│ │                        │ │
│ └────────────────────────┘ │
│                            │
│ [取消] [导入(12个词汇)]     │
└────────────────────────────┘
```

- [ ] 3.3.1 支持逗号、换行符分隔
- [ ] 3.3.2 实时统计词汇数量
- [ ] 3.3.3 去重提示
- [ ] 3.3.4 导入成功反馈

**文件**：`admin/src/components/WordImportDialog.tsx`

#### Task 3.4: AI生成进度组件
**组件**：`admin/src/components/WordGenerationProgress.tsx`

```
┌────────────────────────────┐
│ 正在生成词条...             │
├────────────────────────────┤
│ 进度：3/10                  │
│ ████████░░░░░░░░░ 30%      │
│                            │
│ ✓ 学校 - 已生成             │
│ ✓ 老师 - 已生成             │
│ ⏳ 学生 - 生成中...         │
│ ⏸ 书 - 等待中              │
│ ⏸ 桌子 - 等待中            │
│                            │
│ 预计剩余时间：14分钟        │
│                            │
│ [暂停] [取消]               │
└────────────────────────────┘
```

- [ ] 3.4.1 实时进度条
- [ ] 3.4.2 每个词的状态显示
- [ ] 3.4.3 错误提示（失败的词）
- [ ] 3.4.4 成功/失败统计

**文件**：`admin/src/components/WordGenerationProgress.tsx`

#### Task 3.5: 词条预览页面
**页面**：`admin/src/pages/WordPreview.tsx`

- [ ] 3.5.1 渲染HTML（基于`backend/src/seo2.html`样式）
- [ ] 3.5.2 显示SEO评分
- [ ] 3.5.3 显示字数统计
- [ ] 3.5.4 [编辑JSON] 按钮（高级功能）
- [ ] 3.5.5 [发布] 按钮

**文件**：`admin/src/pages/WordPreview.tsx`

#### Task 3.6: JSON编辑器（可选）
**组件**：`admin/src/components/WordJsonEditor.tsx`

- [ ] 3.6.1 使用Monaco Editor或CodeMirror
- [ ] 3.6.2 JSON语法高亮
- [ ] 3.6.3 实时校验
- [ ] 3.6.4 保存更新

**文件**：`admin/src/components/WordJsonEditor.tsx`

---

### 阶段4：用户端UI开发（4小时）

#### Task 4.1: 删除学习中心模块
- [ ] 4.1.1 删除`frontend-user/app/dashboard/learn`目录
- [ ] 4.1.2 更新导航菜单

**文件**：
- `frontend-user/app/dashboard/layout.tsx`
- 相关导航组件

#### Task 4.2: 改造HSK词汇库
**页面**：`frontend-user/app/dashboard/hsk-library/level/[level]/page.tsx`

- [ ] 4.2.1 只显示已发布的词条（status=PUBLISHED）
- [ ] 4.2.2 点击词汇跳转到`/word/[slug]`

**文件**：`frontend-user/app/dashboard/hsk-library/level/[level]/page.tsx`

#### Task 4.3: 改造词条详情页
**页面**：`frontend-user/app/word/[slug]/page.tsx`

- [ ] 4.3.1 从新API获取数据（`/api/words/:slug`）
- [ ] 4.3.2 渲染完整词条内容（基于contentJson）
- [ ] 4.3.3 添加学习工具：
  - 添加到词库按钮
  - 学习状态显示
  - 标记为已掌握按钮
- [ ] 4.3.4 音频播放
- [ ] 4.3.5 练习题交互

**文件**：
- `frontend-user/app/word/[slug]/page.tsx`
- `frontend-user/components/word/WordEntryRenderer.tsx`（新建）

#### Task 4.4: 我的词库页面
**页面**：`frontend-user/app/dashboard/my-words/page.tsx`

**UI布局**：
```
┌─────────────────────────────────────────┐
│ 我的词库                                 │
├─────────────────────────────────────────┤
│ [待学习(5)] [学习中(2)] [已掌握(12)]     │
├─────────────────────────────────────────┤
│ ┌─────────┬─────────┬─────────┐         │
│ │ 学校    │ 老师    │ 学生    │         │
│ │ xuexiao │ laoshi  │ xuesheng│         │
│ │ HSK1    │ HSK1    │ HSK1    │         │
│ │[学习]   │[学习]   │[学习]   │         │
│ └─────────┴─────────┴─────────┘         │
└─────────────────────────────────────────┘
```

- [ ] 4.4.1 三个Tab切换（待学习、学习中、已掌握）
- [ ] 4.4.2 卡片展示
- [ ] 4.4.3 点击[学习]跳转到词条详情页

**文件**：`frontend-user/app/dashboard/my-words/page.tsx`

#### Task 4.5: 文章/文本分析添加到词库
**页面**：
- `frontend-user/app/dashboard/articles/[slug]/page.tsx`
- `frontend-user/app/dashboard/text-analyzer/page.tsx`

- [ ] 4.5.1 词汇卡片添加[+]按钮
- [ ] 4.5.2 点击后调用API添加到词库（status=PENDING）
- [ ] 4.5.3 显示"已添加"状态

**文件**：
- `frontend-user/app/dashboard/articles/[slug]/page.tsx`
- `frontend-user/app/dashboard/text-analyzer/page.tsx`

---

### 阶段5：内容渲染组件（3小时）

#### Task 5.1: 词条HTML渲染器
**组件**：`frontend-user/components/word/WordEntryRenderer.tsx`

基于`backend/src/seo2.html`的样式，渲染JSON内容：

- [ ] 5.1.1 Hero区（词汇、拼音、英文、HSK等级）
- [ ] 5.1.2 核心含义（中英文definition）
- [ ] 5.1.3 常见搭配（collocations）
- [ ] 5.1.4 详细说明（detailedExplanation）
- [ ] 5.1.5 易错点（confusionPoints）
- [ ] 5.1.6 真题例句（examples）
- [ ] 5.1.7 练习题（practiceQuestions）
- [ ] 5.1.8 相关词汇（relatedWords）
- [ ] 5.1.9 文化背景（culturalTips）
- [ ] 5.1.10 CTA区（底部引导）

**文件**：`frontend-user/components/word/WordEntryRenderer.tsx`

#### Task 5.2: 练习题交互组件
**组件**：`frontend-user/components/word/PracticeQuiz.tsx`

- [ ] 5.2.1 单选题交互
- [ ] 5.2.2 答案验证
- [ ] 5.2.3 正确/错误反馈
- [ ] 5.2.4 解析显示

**文件**：`frontend-user/components/word/PracticeQuiz.tsx`

---

### 阶段6：测试与优化（2小时）

#### Task 6.1: 功能测试
- [ ] 6.1.1 批量导入10个词汇
- [ ] 6.1.2 生成词条（验证JSON格式）
- [ ] 6.1.3 预览词条
- [ ] 6.1.4 发布词条
- [ ] 6.1.5 用户端访问词条
- [ ] 6.1.6 添加到词库
- [ ] 6.1.7 标记已掌握

#### Task 6.2: 性能优化
- [ ] 6.2.1 生成API添加超时控制（150秒）
- [ ] 6.2.2 前端添加loading状态
- [ ] 6.2.3 数据库索引优化

#### Task 6.3: 错误处理
- [ ] 6.3.1 AI生成失败重试机制
- [ ] 6.3.2 JSON格式错误友好提示
- [ ] 6.3.3 网络错误处理

---

## 🎯 开发顺序

### 第1天（8小时）：
1. ✅ Task 1.1-1.2: 数据库重构
2. ✅ Task 2.1: 导入API
3. ✅ Task 2.2: AI生成API
4. ✅ Task 2.3-2.5: 管理端API

### 第2天（8小时）：
5. ✅ Task 3.1-3.2: 管理端基础页面
6. ✅ Task 3.3-3.4: 导入和生成UI
7. ✅ Task 3.5: 预览页面

### 第3天（7小时）：
8. ✅ Task 2.6-2.8: 用户端API
9. ✅ Task 4.1-4.3: 用户端词条页面
10. ✅ Task 5.1-5.2: 渲染组件

### 第4天（2小时）：
11. ✅ Task 4.4-4.5: 我的词库
12. ✅ Task 6.1-6.3: 测试优化

---

## 📝 注意事项

1. **JSON存储格式**：contentJson字段存储完整的AI生成JSON，包含所有字段
2. **SEO保留**：词条仍然需要良好的SEO（title、description、canonical）
3. **音频文件**：音频路径`/audio/hsk1/xuexiao.mp3`需要预先准备或后续生成
4. **AI提示词**：使用`backend/src/词条规则`作为模板，替换`[替换为目标词汇]`
5. **状态流转**：严格控制状态变化，防止状态混乱
6. **并发控制**：批量生成时控制并发数（建议3-5个）
7. **错误重试**：AI生成失败时自动重试3次
8. **数据备份**：每次重大操作前备份数据库

---

## 🚀 准备好了吗？

请确认以上计划，我们开始执行！

**建议先执行前3个任务验证可行性：**
1. Task 1.1: 创建数据库schema
2. Task 2.1: 导入API
3. Task 2.2: AI生成API

然后我们再继续后面的任务。你同意吗？




